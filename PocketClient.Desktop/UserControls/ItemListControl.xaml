<!-- Copyright (c) Microsoft Corporation. All rights reserved. -->
<!-- Licensed under the MIT License. See LICENSE in the project root for license information. -->

<UserControl
    x:Class="PocketClient.Desktop.UserControls.ItemListControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PocketClient.Desktop.UserControls"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:models="using:PocketClient.Core.Models"
    xmlns:converters="using:PocketClient.Desktop.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <UserControl.Resources>
        <converters:PocketItemTitleConverter x:Key="pocketItemTitleConverter" />
        
        <DataTemplate x:Key="ItemTemplate" x:DataType="models:PocketItem">
            <Grid Margin="6,12,12,12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <TextBlock Text="{x:Bind Converter={StaticResource pocketItemTitleConverter}}"
                           MaxLines="2"
                           Style="{StaticResource ListTitleStyle}" />
                <TextBlock Grid.Row="1" 
                           Text="{x:Bind Excerpt}"
                           Style="{StaticResource ListSubTitleStyle}" 
                           MaxLines="3" />
                <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                        <TextBlock TextAlignment="Left" Text="{x:Bind WordCount}" />
                        <TextBlock TextAlignment="Left" Text=" words" />
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" >
                        <TextBlock TextAlignment="Right" Text="{x:Bind TimeToRead}" />
                        <TextBlock TextAlignment="Left" Text=" mins" />
                    </StackPanel>
                </Grid>
            </Grid>
        </DataTemplate>

        <DataTemplate x:Key="NoSelectionContentTemplate">
            <Grid>
                <TextBlock
                        x:Uid="MyList_NoSelection"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        TextAlignment="Center"
                        Style="{ThemeResource SubtitleTextBlockStyle}" />
            </Grid>
        </DataTemplate>

        <DataTemplate x:Key="ListHeaderTemplate">
            <Grid Height="40">
                <TextBlock
                        VerticalAlignment="Center"
                        Style="{StaticResource ListTitleStyle}"
                        Text="{Binding}" />
            </Grid>
        </DataTemplate>
        
        <DataTemplate x:Key="MinimalListHeaderTemplate">
            <Grid Height="40">
                <TextBlock
                        Margin="96,0,0,0"
                        VerticalAlignment="Center"
                        Style="{StaticResource ListTitleStyle}"
                        Text="{Binding}" />
            </Grid>
        </DataTemplate>
    </UserControl.Resources>

    <Grid x:Name="ContentArea">
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState>
                    <VisualState.StateTriggers>
                        <!--641 is the default CompactModeThresholdWidth in NavigationView -->
                        <AdaptiveTrigger MinWindowWidth="641" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="ListDetailsViewControl.ListHeaderTemplate" Value="{StaticResource ListHeaderTemplate}" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
        <controls:ListDetailsView
            x:Uid="MyList"
            x:Name="ListDetailsViewControl"
            BackButtonBehavior="Manual"
            Background="Transparent"
            BorderBrush="Transparent"
            DetailsTemplate="{x:Bind DetailsTemplate}"
            ItemsSource="{x:Bind ItemsSource}"
            ItemTemplate="{StaticResource ItemTemplate}"
            ListHeaderTemplate="{StaticResource MinimalListHeaderTemplate}"
            NoSelectionContentTemplate="{StaticResource NoSelectionContentTemplate}"
            SelectedItem="{x:Bind SelectedItem, Mode=TwoWay}"
            ViewStateChanged="OnViewStateChanged" />
    </Grid>
</UserControl>
